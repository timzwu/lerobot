# π0.5 fine-tune: results (2026-09-12)

Checkpoint `pi05_so101_blocks_40000_ep0-49_100-149` (40k × 32 = 24 passes over the 100 pair episodes, matched to
SmolVLA-100; vision encoder frozen, action expert trained; H100, two attempts, ≈13 h of GPU in total). Served from a
Modal L40S through the stop-and-go client (`eval.py --mode sync --server`), 50-step chunks, 30 s of motion per trial,
think pauses 1–2.5 s (network + inference), motion at a measured 27 Hz with recording on.

| pass | success | 95% interval | red | blue | buckets |
|---|---|---|---|---|---|
| trained pair (`pi05_pair.csv`) | 5/20 (25%) | 11–47% | 2/10 | 3/10 | touch_no_grip 11, no_reach 4 |
| all four combos (`pi05_all4.csv`) | 2/20 (10%) | 3–30% | red→left 1/5, red→right 1/5 | blue→left 0/5, blue→right 0/5 | touch_no_grip 13, no_reach 5 |

Reading: reaching is fine (16/20 touched the block on the pair), grasping is not (5 of those 16 converted; SmolVLA converts
10 of 17). The notes say the same thing twenty times: "touches block but grippers not positioned correctly", "positioned
correctly but grippers don't close", "attempts to grip at offset". Two successes were called lucky in the notes (the jaws
"happen to" land around the block). No wrong-bowl failures on the four-combo pass, but with two successes that says little.
Stage scoring: never reached 4, touched 11, grasped-then-lost 0, success 5 → 1.30 / 3 (SmolVLA-100: 1.80).

Hypotheses and how to test them are in `comparison_wk1.md` (π0.5 section). Videos of all 40 trials in `trials/`
(`pi05_pair_NN.mp4`, `pi05_all4_NN.mp4`; 30 s of motion each, think pauses cut).

Protocol notes for this pass: the first eval attempt ran the chunks at ~120 Hz because the recording path skipped the
per-step pacing (bug introduced Sept 11, caught on trial 1, fixed; that trial was discarded and no earlier pass was
recorded). The eval driver's per-trial budget was raised to cover the server's 100–140 s model load; the server keeps the
loaded model across trials.
